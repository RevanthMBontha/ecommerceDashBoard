import Login from '../components/Login';

const Auth = () => {
  return (
    <div className="relative flex items-center justify-center w-full h-full bg-gray-200 custom-shadow">
      <Login />
    </div>
  );
};

export default Auth;
